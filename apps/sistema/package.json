{
  "name": "sistema",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "react-scripts": "^2.0.5"
  },
  "proxy": "http://localhost:8080",
  "homepage": "sistema/build/",
  "dependencies": {
    "react": "^16.1.1",
    "react-dom": "^16.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && rm -rf ../../src/main/webapp/sistema/build/ && mv build/ ../../src/main/webapp/sistema/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
